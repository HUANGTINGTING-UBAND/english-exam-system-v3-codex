# AGENTS.md

## 项目定位

本仓库是英语在线考试系统，当前需要逐步升级为支持多学段英语试卷导入、在线考试、学情诊断与教学支持的平台。

后续开发必须兼顾：

- 小学 / 小升初 / 初中 / 高中 / 大学英语四级等多学段英语试卷。
- TXT / DOCX / PDF 上传与解析。
- 试卷原题、答案、答案解析、听力材料、听力原文、图片素材。
- 听力题、图片题、选择题、填空题、阅读题、翻译题、写作题等题型。
- 上传后先生成“导入草稿”，人工校对后再入库。
- 学生考试、自动评分、错题本、学情报告。
- 教师 / 管理员查看班级统计、讲评建议、补充练习。

## 当前仓库事实边界

- 当前前端业务代码位于 `frontend/src`。
- 当前后端业务代码位于 `backend/src` 与 `backend/index.js`。
- 当前数据库 schema 位于 `backend/prisma/schema.prisma`。
- 当前项目已具备学生注册登录、试卷列表、在线考试、考试提交、结果详情、错题本、管理员试卷题目管理、题目导入入口等 MVP 能力。
- 任何尚未在代码中明确存在的功能，文档和回复中必须标记为 `NEEDS_VERIFICATION`，不得臆造。

## 开发规则

1. 正式开发前，先阅读并更新必要上下文文档：
   - `docs/codex-context/ENGLISH_EXAM_STRUCTURE.md`
   - `docs/codex-context/IMPORT_FILE_FORMAT_SPEC.md`
   - `docs/codex-context/IMPORT_TEST_CASES.md`
   - `docs/codex-context/PROJECT_STATUS_GAP_ANALYSIS.md`
2. 未经明确要求，不要改动：
   - 前端业务代码；
   - 后端业务代码；
   - `backend/prisma/schema.prisma`；
   - `.env` 或真实密钥；
   - `package.json` / `package-lock.json`；
   - 部署配置。
3. 需要新增字段、表、接口、页面时，先在上下文文档中说明目标结构、迁移影响、兼容策略和测试要求。
4. 导入链路必须遵循“解析 -> 导入草稿 -> 人工校对 -> 入库”的方向设计，不应直接把不可信解析结果写入正式题库。
5. 涉及 AI 解析、OCR、PDF、音频、图片等能力时，应明确失败兜底、人工校对入口、原始文本留存和错误提示。
6. 不要在 import 外层包裹 try/catch。
7. 代码变更后必须运行与变更相关的最小测试；如果测试因环境限制无法完成，说明限制和命令输出。

## 文档书写规则

- 区分“仓库已实现事实”和“目标需求 / 规划建议”。
- 对不确定功能使用 `NEEDS_VERIFICATION`。
- 文件格式、题型、评分规则等规范必须给出最小可测样例。
- 上下文文档应服务后续开发，而不是替代业务实现。
